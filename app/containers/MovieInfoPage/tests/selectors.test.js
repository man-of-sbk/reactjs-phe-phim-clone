// import { selectMovieInfoPageDomain } from '../selectors';

describe('selectMovieInfoPageDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
